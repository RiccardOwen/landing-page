# Landing Page Template

A minimal, responsive landing page template.

Getting started

- Open `index.html` in a browser

Project structure

- `index.html` — main page
- `styles.css` — styles
- `script.js` — small interactive helpers

Customize

Change colors in `styles.css` using CSS variables at the top.
